import { renderConfirmationPrompt, renderWarning } from '@shopify/cli-kit/node/ui';
export function themeComponent(theme) {
    return [
        `'${theme.name}'`,
        {
            subdued: `(#${theme.id})`,
        },
    ];
}
export function themesComponent(themes) {
    const items = themes.map(themeComponent);
    return { list: { items } };
}
export async function currentDirectoryConfirmed(force) {
    if (force) {
        return true;
    }
    renderWarning({ body: `It doesn't seem like you're running this command in a theme directory.` });
    if (!process.stdout.isTTY) {
        return true;
    }
    return renderConfirmationPrompt({
        message: 'Do you want to proceed?',
    });
}
//# sourceMappingURL=theme-ui.js.map