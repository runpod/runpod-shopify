import { AdminSession } from '@shopify/cli-kit/node/session';
export interface RenameOptions {
    newName: string;
    development?: boolean;
    theme?: string;
    live?: boolean;
}
export declare function renameTheme(adminSession: AdminSession, options: RenameOptions): Promise<void>;
