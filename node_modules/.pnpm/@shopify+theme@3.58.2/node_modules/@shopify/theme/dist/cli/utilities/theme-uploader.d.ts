import { AdminSession } from '@shopify/cli-kit/node/session';
import { Result, Checksum, Theme, ThemeFileSystem } from '@shopify/cli-kit/node/themes/types';
interface UploadOptions {
    path: string;
    nodelete?: boolean;
    ignore?: string[];
    only?: string[];
}
export declare const MAX_BATCH_FILE_COUNT = 10;
export declare const MAX_BATCH_BYTESIZE = 102400;
export declare const MAX_UPLOAD_RETRY_COUNT = 2;
export declare function uploadTheme(theme: Theme, session: AdminSession, remoteChecksums: Checksum[], themeFileSystem: ThemeFileSystem, options: UploadOptions): Promise<Map<string, Result>>;
export {};
