import { AdminSession } from '@shopify/cli-kit/node/session';
export interface DevOptions {
    adminSession: AdminSession;
    storefrontToken: string;
    directory: string;
    store: string;
    password?: string;
    open: boolean;
    theme: string;
    host?: string;
    port?: string;
    force: boolean;
    flagsToPass: string[];
}
export declare function dev(options: DevOptions): Promise<void>;
export declare function renderLinks(store: string, themeId: string, host?: string, port?: string): void;
export declare function showDeprecationWarnings(args: string[]): void;
export declare function refreshTokens(store: string, password: string | undefined): Promise<{
    adminSession: AdminSession;
    storefrontToken: string;
}>;
